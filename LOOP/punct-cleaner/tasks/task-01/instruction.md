# task-01 instruction

TODO: replace this placeholder with a single concrete sanitization task.

Describe what the cleaner should do, as if instructing a worker. Example shape:

> "Take the markdown article in `input/article.md` and produce reader-ready
> text suitable for TTS playback. Strip markdown syntax (asterisks, underscores,
> headings, blockquotes, table pipes, etc.) and convert em-dashes / double-hyphens
> to comma pauses. Preserve sentence-level punctuation."

Optional: create `input/` here and drop the source files the task references.
