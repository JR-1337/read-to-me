# task-01 expected result

TODO: replace this placeholder with what a successful result looks like.

Be concrete. Examples by metric type:

- **Exact match**: paste the exact cleaned text the cleaner should produce.
- **Property check**: list properties that must hold (e.g. "no `*` characters in output", "every `--` replaced with `, `").
- **LLM judge criteria**: list judging rubric items (e.g. "reads naturally aloud", "no markdown syntax remaining").

`score.sh` translates this expected result into the scalar `sanitize_pass_rate`.
